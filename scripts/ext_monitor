#!/bin/sh

ext_monitor=$(xrandr --query | rg 'HDMI-1-1')
if [[ $ext_monitor = *connected* ]]; then
      xrandr --output eDP-1 --primary --mode 1920x1080 --rotate normal --output HDMI-1-1 --mode 1920x1080 --rotate normal --right-of eDP-1
fi
